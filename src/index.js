import React from 'react';
import ReactDOM from 'react-dom';

import App from './App'

import './index.css';

ReactDOM.render(<App />, document.getElementById('root')) //первый параметр - сам элемент(что хотим рендерить в ДОМ)б второй элемент - место, куда хотим рендерит